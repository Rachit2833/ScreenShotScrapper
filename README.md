This is a Screen Shot Scrapper script using puppeteer the only dependancy is puppeteer 
    you can run the script by 
       clone the repo and install the dependency by 
        npm i
        node index.js 
        or npm run start
        
